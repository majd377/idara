const $=s=>document.querySelector(s), $$=s=>[...document.querySelectorAll(s)];
const state={view:'dashboard',subscribers:[],periods:[],sources:[]};
const fmt=(n,d=2)=>new Intl.NumberFormat('ar-PS',{maximumFractionDigits:d}).format(Number(n||0));
const money=n=>`${fmt(n,2)} ₪`;
const safe=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]));
async function api(url,opts={}){if(window.__AMIN_STATIC_DEMO__&&window.__AMIN_STATIC_API__)return window.__AMIN_STATIC_API__(url,opts);const r=await fetch(url,opts);let j={};try{j=await r.json()}catch{}if(!r.ok)throw Error(j.error||'حدث خطأ غير متوقع');return j}
function render(h){$('#app').innerHTML=h}
function setTitle(t,sub=''){const names={dashboard:'الرئيسية',subscribers:'السكان',periods:'قراءات الماء',costs:'المصاريف',payments:'الدفعات',reports:'التقارير'};$('#page-title').textContent=t;$('#page-subtitle').textContent=sub;$('#crumb').textContent=names[state.view]||t}
function toast(m,type='success'){const x=$('#toast');x.textContent=m;x.className=`toast ${type}`;clearTimeout(toast.t);toast.t=setTimeout(()=>x.className='toast hidden',3200)}
function badge(s){const a={Draft:'مسودة',DataEntry:'إدخال',Calculated:'محسوبة',Review:'مراجعة',Approved:'معتمدة',Closed:'مغلقة',Pending:'بانتظار',Entered:'مدخلة','No Consumption':'لا استهلاك',Invalid:'غير صالحة'};const c=['Closed','Approved','Calculated','Entered'].includes(s)?'success':['Pending','Draft','Review','DataEntry'].includes(s)?'warning':s==='Invalid'?'danger':'info';return `<span class="badge ${c}">${safe(a[s]||s||'-')}</span>`}
function fmtDate(v){return !v?'-':String(v).split('-').reverse().join('/')}
function exportFile(url,name){
  if(window.__AMIN_STATIC_DEMO__&&window.__AMIN_STATIC_EXPORT__){window.__AMIN_STATIC_EXPORT__(url,name).then?.(()=>toast(window.__AMIN_EXPORT_NOTE__||'تم التصدير'));return}
  fetch(url).then(async r=>{if(!r.ok){let j={};try{j=await r.json()}catch{}throw Error(j.error||'تعذر التصدير')}return r.blob()}).then(b=>{const a=document.createElement('a');a.href=URL.createObjectURL(b);a.download=name;document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(a.href),900);toast('تم تنزيل ملف Excel')}).catch(e=>toast(e.message||'تعذر التصدير','error'))
}

async function dashboard(){
 state.view='dashboard';setTitle('الرئيسية','ابدأ من هنا: اختَر المهمة التي تريدها، والباقي على البرنامج.');
 const d=await api('/api/dashboard'),ps=d.periods||[],latest=ps[0]; let s=null;if(latest){try{s=await api('/api/periods/'+latest.id+'/summary')}catch{}}
 render(`<div class="card hero"><h2>أهلًا بكم في عمارة الأمين 👋</h2><div class="hero-number">${latest?safe(latest.label):'جاهز لبدء أول أسبوع'}</div><div class="hero-meta">${latest?`<span>التاريخ: <strong>${fmtDate(latest.start_date)}</strong></span><span>الحالة: ${badge(latest.status)}</span>`:'أدخل أول القراءات وسيحسب النظام كل شيء.'}</div>${s?`<div class="kpis" style="margin-top:18px"><div class="summary-box"><div class="label">تكلفة التشغيل</div><div class="value">${money(s.netOperational)}</div></div><div class="summary-box"><div class="label">استهلاك الماء</div><div class="value">${fmt(s.totalConsumption,3)}</div></div><div class="summary-box"><div class="label">سعر الكوب</div><div class="value">${money(s.appliedUnitPrice)}</div></div><div class="summary-box"><div class="label">فرق التقريب</div><div class="value">${money(s.roundingDifference)}</div></div></div>`:''}</div>
 <div class="steps" style="margin-top:13px"><div class="step"><div class="step-num">1</div><div><strong>افتح الأسبوع</strong><span>أنشئ أسبوعًا جديدًا أو افتح آخر أسبوع.</span></div></div><div class="step"><div class="step-num">2</div><div><strong>أدخل الحالية</strong><span>السابقة موجودة ويمكنك تعديلها وقت الحاجة.</span></div></div><div class="step"><div class="step-num">3</div><div><strong>احسب وصدّر</strong><span>راجع الحساب ثم نزّل تقرير Excel.</span></div></div></div>
 <div class="kpis" style="margin-top:13px"><div class="card kpi"><div class="label">عدد السكان</div><div class="value">${fmt(d.subscribers,0)}</div><div class="hint">مشترك نشط</div></div><div class="card kpi"><div class="label">الاستحقاقات</div><div class="value">${money(d.totalCharges)}</div><div class="hint">إجمالي المبالغ</div></div><div class="card kpi"><div class="label">الدفعات</div><div class="value">${money(d.totalPayments)}</div><div class="hint">المبالغ المستلمة</div></div><div class="card kpi kpi-accent"><div class="label">المتبقي</div><div class="value">${money(d.outstanding)}</div><div class="hint">بحاجة للتحصيل</div></div></div>
 <div class="card section" style="margin-top:13px"><div class="section-head"><div><h2>أكثر الأشياء استخدامًا</h2><p>اختَر زرًا وابدأ مباشرة.</p></div></div><div class="quick"><button id="homeRead"><div class="q-icon">💧</div><b>إدخال قراءات الماء</b><span>افتح آخر أسبوع وابدأ الإدخال.</span></button><button id="homePeriod"><div class="q-icon">📅</div><b>إنشاء أسبوع جديد</b><span>ابدأ دورة حساب جديدة.</span></button><button id="homePay"><div class="q-icon">💵</div><b>تسجيل دفعة</b><span>أضف دفعة لأحد السكان.</span></button><button id="homeReport"><div class="q-icon">📊</div><b>التقارير وExcel</b><span>نزّل تقريرًا جاهزًا لفتحه في Excel.</span></button></div></div>
 <div class="data-note" style="margin-top:13px"><span class="mini">💾</span><div><strong>الحفظ المحلي:</strong> في نسخة GitHub التجريبية تبقى البيانات في نفس المتصفح والجهاز. تشغيل البرنامج محليًا يستخدم SQLite حقيقية على الجهاز.</div></div>`);
 $('#homeRead').onclick=()=>latest?periodDetail(latest.id):showPeriodForm();$('#homePeriod').onclick=showPeriodForm;$('#homePay').onclick=showPaymentForm;$('#homeReport').onclick=reports;
}

async function subscribers(){
 state.view='subscribers';setTitle('السكان','أضف، عدّل، وابحث عن أي ساكن بسهولة.');state.subscribers=await api('/api/subscribers');
 render(`<div class="card section"><div class="section-head"><div><h2>السكان</h2><p>كل ساكن له حساب، ويمكنك تعديل بياناته في أي وقت.</p></div><div class="inline-actions"><button class="btn btn-ghost" id="exportSubs">⬇ تصدير Excel</button><button class="btn btn-primary" id="newSub">＋ إضافة ساكن</button></div></div><div class="toolbar"><input id="subSearch" class="search" placeholder="ابحث بالاسم أو الكود أو الهاتف..."><span class="muted">${fmt(state.subscribers.length,0)} ساكن</span></div><div class="table-wrap" style="margin-top:12px"><table class="table"><thead><tr><th>الكود</th><th>الاسم</th><th>البناية</th><th>الوحدة</th><th>الهاتف</th><th>الرصيد</th><th>إجراءات</th></tr></thead><tbody id="subRows">${subRows(state.subscribers)}</tbody></table></div></div>`);
 $('#newSub').onclick=()=>showSubForm();$('#exportSubs').onclick=()=>exportFile('/api/export/subscribers.xlsx','المشتركون.xlsx');$('#subSearch').oninput=async e=>$('#subRows').innerHTML=subRows(await api('/api/subscribers?q='+encodeURIComponent(e.target.value)));
}
function subRows(data){return data.map(s=>`<tr><td class="strong">${safe(s.code)}</td><td class="strong">${safe(s.name)}</td><td>${safe(s.building_name||'-')}</td><td>${safe(s.unit_code||'-')}</td><td>${safe(s.phone||'-')}</td><td class="strong">${money(s.balance)}</td><td><div class="inline-actions"><button class="btn btn-soft btn-view-sub" data-id="${s.id}">كشف الحساب</button><button class="btn btn-gold btn-edit-sub" data-id="${s.id}">تعديل</button></div></td></tr>`).join('')||`<tr><td colspan="7" class="empty"><strong>لا يوجد سكان بهذا البحث</strong>جرّب كلمة أخرى.</td></tr>`}

async function periods(){
 state.view='periods';setTitle('قراءات الماء','هذه هي الصفحة التي ستستخدمها كل أسبوع.');const ps=await api('/api/periods');state.periods=ps;const latest=ps[0];
 render(`<div class="card section"><div class="section-head"><div><h2>أسابيع الحساب</h2><p>افتح الأسبوع، أدخل القراءات، ثم احسب.</p></div><div class="export-row"><button class="btn btn-ghost" id="newPeriodTop">＋ أسبوع جديد</button>${latest?`<button class="btn btn-primary" id="latestRead">فتح آخر أسبوع</button>`:''}</div></div>${ps.length?`<div class="table-wrap"><table class="table"><thead><tr><th>الأسبوع</th><th>التاريخ</th><th>الحالة</th><th>الإجراء</th></tr></thead><tbody>${ps.map(p=>`<tr><td class="strong">${safe(p.label)}</td><td>${fmtDate(p.start_date)}</td><td>${badge(p.status)}</td><td class="inline-actions"><button class="btn btn-soft open-period" data-id="${p.id}">إدخال القراءات</button><button class="btn btn-ghost calc-period" data-id="${p.id}">عرض الحساب</button><button class="btn btn-ghost export-period" data-id="${p.id}">Excel</button></td></tr>`).join('')}</tbody></table></div>`:`<div class="empty"><strong>لا يوجد أسبوع حتى الآن</strong>اضغط «أسبوع جديد» للبدء.</div>`}</div>`);
 $('#newPeriodTop').onclick=showPeriodForm;if(latest)$('#latestRead').onclick=()=>periodDetail(latest.id);$$('.open-period').forEach(b=>b.onclick=()=>periodDetail(b.dataset.id));$$('.calc-period').forEach(b=>b.onclick=()=>periodSummary(b.dataset.id));$$('.export-period').forEach(b=>b.onclick=()=>exportFile('/api/export/period/'+b.dataset.id+'.xlsx','تقرير-'+b.dataset.id+'.xlsx'));
}
async function periodDetail(id){
 const readings=await api('/api/readings/'+id),meters=await api('/api/meters'),p=(await api('/api/periods')).find(x=>Number(x.id)===Number(id));if(!meters.length){openModal('<h2>لا توجد عدادات</h2><p class="muted">أضف السكان والعدادات أولًا.</p>');return}
 const map=new Map(readings.map(r=>[Number(r.meter_id),r]));setTitle('قراءات الماء',p?`أدخل القراءة السابقة والحالية لـ ${p.label}.`:'إدخال القراءات');
 render(`<div class="card section"><div class="section-head"><div><h2>${safe(p?.label||'الأسبوع')}</h2><p>لا تحتاج إلى حساب الاستهلاك بنفسك.</p></div><div class="inline-actions"><button class="btn btn-ghost" id="backPeriods">عودة</button><button class="btn btn-ghost" id="exportCurrent">⬇ Excel</button><button class="btn btn-primary" id="calcCurrent">احسب الأسبوع</button></div></div><div class="save-all"><div><strong>أسهل طريقة</strong><span>عدّل «السابقة» فقط إذا احتجت، ثم أدخل «الحالية». لا تحسب شيئًا يدويًا.</span></div><button class="btn btn-primary" id="saveAll">حفظ القراءات</button></div><div class="table-wrap"><table class="table reading-table"><thead><tr><th>الكود</th><th>الاسم</th><th>القراءة السابقة</th><th>القراءة الحالية</th><th>الاستهلاك</th><th>الحالة</th></tr></thead><tbody>${meters.map(m=>{const r=map.get(Number(m.id))||{};return `<tr><td class="strong">${safe(m.subscriber_code||'-')}</td><td class="strong">${safe(m.subscriber_name||'-')}</td><td><input class="prev-reading" data-meter="${m.id}" value="${safe(r.previous_reading??'')}" inputmode="decimal" type="number" step="0.001" placeholder="مثلاً 125000"></td><td><input class="current-reading input-current" data-meter="${m.id}" value="${safe(r.current_reading??'')}" inputmode="decimal" type="number" step="0.001" placeholder="أدخل الحالية"></td><td>${r.consumption==null?'-':fmt(r.consumption,3)}</td><td>${badge(r.status||'Pending')}</td></tr>`}).join('')}</tbody></table></div></div>`);
 $('#backPeriods').onclick=periods;$('#calcCurrent').onclick=()=>periodSummary(id);$('#exportCurrent').onclick=()=>exportFile('/api/export/period/'+id+'.xlsx','قراءات-'+id+'.xlsx');$('#saveAll').onclick=async()=>{const rows=$$('.current-reading');let saved=0,failed=0;for(const input of rows){const meterId=input.dataset.meter;const prev=document.querySelector(`.prev-reading[data-meter="${meterId}"]`).value;const current=input.value;if(prev===''&&current==='')continue;try{await api('/api/readings',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({periodId:id,meterId,previous:prev,current})});saved++}catch{failed++}}toast(failed?`تم حفظ ${saved} قراءة، وهناك ${failed} تحتاج مراجعة.`:`تم حفظ ${saved} قراءة.`);await periodDetail(id)};
}
async function periodSummary(id){const s=await api('/api/periods/'+id+'/summary');openModal(`<div class="account-top"><div><h2>ملخص الأسبوع</h2><div class="account-id">${safe(s.period.label)} — ${fmtDate(s.period.start_date)}</div></div><div class="summary-box"><div class="label">سعر الكوب</div><div class="balance-large">${money(s.appliedUnitPrice)}</div></div></div><div class="kpis" style="margin-top:16px"><div class="summary-box"><div class="label">تكلفة التشغيل</div><div class="value">${money(s.netOperational)}</div></div><div class="summary-box"><div class="label">الاستهلاك</div><div class="value">${fmt(s.totalConsumption,3)}</div></div><div class="summary-box"><div class="label">السعر الخام</div><div class="value">${fmt(s.rawUnitPrice,4)} ₪</div></div><div class="summary-box"><div class="label">فرق التقريب</div><div class="value">${money(s.roundingDifference)}</div></div></div><div class="notice" style="margin-top:16px"><strong>كيف انحسب سعر الكوب؟</strong><br>صافي تكلفة التشغيل ÷ إجمالي الاستهلاك = السعر الخام، ثم يُرفع للعدد الصحيح حسب قاعدة النظام = <strong>${money(s.appliedUnitPrice)}</strong>.</div><div class="actions"><button class="btn btn-primary" id="sumExport">⬇ تقرير Excel</button><button class="btn btn-ghost" id="sumReadings">تعديل القراءات</button></div></div>`);$('#sumExport').onclick=()=>exportFile('/api/export/period/'+id+'.xlsx','تقرير-'+id+'.xlsx');$('#sumReadings').onclick=()=>{closeModal();periodDetail(id)}}

async function costs(){state.view='costs';setTitle('المصاريف','سجّل أي شيء يدخل في تكلفة التشغيل أو المصاريف المشتركة.');state.sources=await api('/api/energy/sources');render(`<div class="grid2"><div class="card section"><div class="section-head"><div><h2>مصادر التشغيل</h2><p>أبو زايد، السويسي، ومولد خارجي عند الحاجة.</p></div><button class="btn btn-primary" id="energyBtn">＋ قراءة مولد</button></div><div class="table-wrap"><table class="table"><thead><tr><th>المصدر</th><th>النوع</th><th>الحالة</th></tr></thead><tbody>${state.sources.map(s=>`<tr><td class="strong">${safe(s.name)}</td><td>${safe(s.source_type)}</td><td>${s.active?badge('Approved'):`<span class="badge info">غير نشط</span>`}</td></tr>`).join('')}</tbody></table></div></div><div class="card section"><div class="section-head"><div><h2>مصروف جديد</h2><p>مولد خارجي، نقل، وقود، صيانة أو طارئ.</p></div><button class="btn btn-primary" id="costBtn">＋ إضافة مصروف</button></div><div class="notice">اختر طريقة تحميل المصروف حتى يعرف النظام هل يدخل في حساب سعر الكوب أم لا.</div></div></div>`);$('#energyBtn').onclick=showEnergyForm;$('#costBtn').onclick=showCostForm}
async function payments(){state.view='payments';setTitle('الدفعات','سجّل الدفعة، وسيظهر أثرها فورًا في حساب الساكن.');const s=await api('/api/subscribers');state.subscribers=s;render(`<div class="card section"><div class="section-head"><div><h2>تحصيل السكان</h2><p>اضغط «كشف الحساب» أو سجّل دفعة جديدة.</p></div><button class="btn btn-primary" id="payBtn">＋ تسجيل دفعة</button></div><div class="table-wrap"><table class="table"><thead><tr><th>الكود</th><th>الاسم</th><th>الرصيد</th><th>الحالة</th><th>إجراء</th></tr></thead><tbody>${s.map(x=>`<tr><td class="strong">${safe(x.code)}</td><td class="strong">${safe(x.name)}</td><td>${money(x.balance)}</td><td>${x.balance>0?`<span class="badge warning">عليه مبلغ</span>`:x.balance<0?`<span class="badge success">رصيد دائن</span>`:`<span class="badge success">مسدد</span>`}</td><td><button class="btn btn-soft btn-view-sub" data-id="${x.id}">كشف الحساب</button></td></tr>`).join('')}</tbody></table></div></div>`);$('#payBtn').onclick=showPaymentForm}
async function reports(){state.view='reports';setTitle('التقارير والنسخ الاحتياطية','كل ما تحتاجه للطباعة والتصدير وحفظ نسخة من بياناتك.');const ps=await api('/api/periods');render(`<div class="card section"><div class="section-head"><div><h2>تصدير السكان</h2><p>بيانات السكان والوحدات والأرصدة.</p></div><button class="btn btn-primary" id="repSubs">⬇ تنزيل Excel</button></div></div><div class="card section" style="margin-top:13px"><div class="section-head"><div><h2>تقارير الأسابيع</h2><p>القراءات والاستهلاك وقيمة المياه.</p></div></div>${ps.length?`<div class="table-wrap"><table class="table"><thead><tr><th>الأسبوع</th><th>التاريخ</th><th>الحساب</th><th>Excel</th></tr></thead><tbody>${ps.map(p=>`<tr><td class="strong">${safe(p.label)}</td><td>${fmtDate(p.start_date)}</td><td><button class="btn btn-soft rep-period" data-id="${p.id}">عرض الحساب</button></td><td><button class="btn btn-ghost exp-period" data-id="${p.id}">⬇ Excel</button></td></tr>`).join('')}</tbody></table></div>`:`<div class="empty"><strong>لا يوجد أسابيع</strong>أنشئ أسبوعًا من صفحة قراءات الماء.</div>`}</div><div class="card section" style="margin-top:13px"><div class="section-head"><div><h2>حماية بيانات الجهاز</h2><p>على GitHub Pages البيانات محفوظة في هذا المتصفح. خذ نسخة احتياطية من وقت لآخر.</p></div><div class="inline-actions"><button class="btn btn-gold" id="backupBtn">⬇ نسخة احتياطية</button><label class="btn btn-ghost" style="cursor:pointer">⬆ استعادة نسخة<input id="restoreInput" type="file" accept="application/json" hidden></label></div></div><div class="notice">مهم: الحسابات داخل نفس المتصفح تستخدم نفس بيانات العمارة. إذا فتحت الموقع في ملف شخصي أو متصفح آخر فسيكون التخزين منفصلًا بسبب نظام المتصفح.</div></div>`);$('#repSubs').onclick=()=>exportFile('/api/export/subscribers.xlsx','المشتركون.xlsx');$$('.rep-period').forEach(b=>b.onclick=()=>periodSummary(b.dataset.id));$$('.exp-period').forEach(b=>b.onclick=()=>exportFile('/api/export/period/'+b.dataset.id+'.xlsx','تقرير-'+b.dataset.id+'.xlsx'));if(window.__AMIN_STATIC_DEMO__){$('#backupBtn').onclick=()=>window.__AMIN_STATIC_BACKUP__();$('#restoreInput').onchange=e=>e.target.files[0]&&window.__AMIN_STATIC_RESTORE__(e.target.files[0]).catch(err=>toast(err.message,'error'))}else{$('#backupBtn').onclick=()=>toast('النسخة المحلية تستخدم ملف SQLite. احتفظ بنسخة من مجلد db/');$('#restoreInput').disabled=true}}

async function showSubForm(id=null){
 let item=null;if(id){item=state.subscribers.find(x=>Number(x.id)===Number(id))||await api('/api/subscribers?q=');if(Array.isArray(item))item=item.find(x=>Number(x.id)===Number(id))}
 const bs=await api('/api/buildings');
 const units=item?await api('/api/units'):[];const unit=item?units.find(u=>Number(u.id)===Number(item.unit_id)):null;
 openModal(`<h2>${item?'تعديل بيانات الساكن':'إضافة ساكن'}</h2><p class="muted">يمكن تعديل الاسم والكود والوحدة والهاتف والخدمات بدون فقدان الحسابات السابقة.</p><form id="subForm"><div class="form-grid"><div class="field"><label>كود الساكن *</label><input name="code" required value="${safe(item?.code||'')}" placeholder="مثال: 1-03"></div><div class="field"><label>الاسم *</label><input name="name" required value="${safe(item?.name||'')}"></div><div class="field"><label>البناية</label><select name="buildingId" id="buildingSelect"><option value="">بدون تحديد</option>${bs.map(b=>`<option value="${b.id}" ${unit&&Number(unit.building_id)===Number(b.id)?'selected':''}>${safe(b.name)}</option>`).join('')}</select></div><div class="field"><label>رقم الوحدة</label><input name="unitCode" value="${safe(unit?.code||item?.unit_code||'')}" placeholder="مثال: 3"></div><div class="field"><label>الدور</label><input name="floor" value="${safe(unit?.floor||'')}"></div><div class="field"><label>رقم الهاتف</label><input name="phone" value="${safe(item?.phone||'')}"></div><div class="field"><label>خدمة الحارس الافتراضية</label><input name="guardFee" type="number" step="0.01" value="${item?.default_guard_fee??30}"></div><div class="field"><label>تأمين الغاطس الافتراضي</label><input name="pumpInsurance" type="number" step="0.01" value="${item?.default_pump_insurance??15}"></div></div><div class="field" style="margin-top:12px"><label>ملاحظات</label><textarea name="notes">${safe(item?.notes||'')}</textarea></div><div class="actions"><button class="btn btn-primary">${item?'حفظ التعديل':'حفظ الساكن'}</button><button type="button" class="btn btn-ghost" onclick="closeModal()">إلغاء</button></div></form>`);
 $('#subForm').onsubmit=async e=>{e.preventDefault();try{const body=Object.fromEntries(new FormData(e.target));const url=item?'/api/subscribers/'+item.id:'/api/subscribers';await api(url,{method:item?'PUT':'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});closeModal();toast(item?'تم حفظ تعديل الساكن':'تمت إضافة الساكن');subscribers()}catch(err){toast(err.message,'error')}}
}
function showPeriodForm(){const t=new Date().toISOString().slice(0,10);openModal(`<h2>إنشاء أسبوع جديد</h2><p class="muted">سيضع النظام آخر قراءة معروفة تلقائيًا كقراءة سابقة لكل عداد.</p><form id="periodForm"><div class="form-grid"><div class="field"><label>اسم الأسبوع *</label><input name="label" required value="أسبوع ${fmtDate(t)}"></div><div class="field"><label>من *</label><input name="startDate" type="date" required value="${t}"></div><div class="field"><label>إلى *</label><input name="endDate" type="date" required value="${t}"></div></div><div class="actions"><button class="btn btn-primary">إنشاء الأسبوع</button><button type="button" class="btn btn-ghost" onclick="closeModal()">إلغاء</button></div></form>`);$('#periodForm').onsubmit=async e=>{e.preventDefault();try{const r=await api('/api/periods',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(new FormData(e.target)))});closeModal();toast('تم إنشاء الأسبوع');periodDetail(r.id)}catch(err){toast(err.message,'error')}}}
function showPaymentForm(){if(!state.subscribers.length){api('/api/subscribers').then(x=>{state.subscribers=x;showPaymentForm()});return}openModal(`<h2>تسجيل دفعة</h2><p class="muted">الدفع يظهر فورًا في كشف الحساب ويخفض الرصيد.</p><form id="paymentForm"><div class="form-grid"><div class="field"><label>الساكن *</label><select name="subscriberId">${state.subscribers.map(s=>`<option value="${s.id}">${safe(s.code)} — ${safe(s.name)}</option>`).join('')}</select></div><div class="field"><label>المبلغ *</label><input name="amount" type="number" step="0.01" min="0.01" required></div><div class="field"><label>التاريخ *</label><input name="date" type="date" value="${new Date().toISOString().slice(0,10)}" required></div><div class="field"><label>طريقة الدفع</label><select name="method"><option value="Cash">نقدي</option><option value="Bank Transfer">تحويل</option><option value="Other">أخرى</option></select></div><div class="field"><label>رقم الإيصال</label><input name="receiptNumber"></div><div class="field"><label>ملاحظة</label><input name="note"></div></div><div class="actions"><button class="btn btn-primary">حفظ الدفعة</button><button type="button" class="btn btn-ghost" onclick="closeModal()">إلغاء</button></div></form>`);$('#paymentForm').onsubmit=async e=>{e.preventDefault();try{await api('/api/payments',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(new FormData(e.target)))});closeModal();toast('تم تسجيل الدفعة');payments()}catch(err){toast(err.message,'error')}}}
async function showEnergyForm(){const ps=await api('/api/periods');openModal(`<h2>إدخال قراءة مولد</h2><form id="energyForm"><div class="form-grid"><div class="field"><label>الأسبوع *</label><select name="periodId">${ps.map(p=>`<option value="${p.id}">${safe(p.label)}</option>`).join('')}</select></div><div class="field"><label>المصدر *</label><select name="sourceId">${state.sources.map(s=>`<option value="${s.id}">${safe(s.name)}</option>`).join('')}</select></div><div class="field"><label>القراءة السابقة</label><input name="previous" type="number" step="0.001"></div><div class="field"><label>القراءة الحالية</label><input name="current" type="number" step="0.001"></div><div class="field"><label>الفاقد</label><input name="loss" type="number" step="0.001" value="0"></div><div class="field"><label>سعر الكيلو</label><input name="price" type="number" step="0.01" min="0"></div></div><div class="actions"><button class="btn btn-primary">حفظ</button><button type="button" class="btn btn-ghost" onclick="closeModal()">إلغاء</button></div></form>`);$('#energyForm').onsubmit=async e=>{e.preventDefault();try{await api('/api/energy/reading',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(new FormData(e.target)))});closeModal();toast('تم حفظ قراءة المولد');costs()}catch(err){toast(err.message,'error')}}}
async function showCostForm(){const ps=await api('/api/periods');openModal(`<h2>إضافة مصروف</h2><form id="costForm"><div class="form-grid"><div class="field"><label>الأسبوع</label><select name="periodId">${ps.map(p=>`<option value="${p.id}">${safe(p.label)}</option>`).join('')}</select></div><div class="field"><label>نوع المصروف</label><select name="type"><option>مولد خارجي</option><option>نقل</option><option>وقود</option><option>صيانة</option><option>طوارئ</option><option>مصروف آخر</option><option>مساهمة/خصم</option></select></div><div class="field"><label>المبلغ *</label><input name="amount" type="number" step="0.01" min="0" required></div><div class="field"><label>طريقة تحميله</label><select name="allocationRule"><option value="WATER_CONSUMPTION">على استهلاك المياه</option><option value="EACH_SUBSCRIBER">بالتساوي على السكان</option><option value="CUSTOM">تخصيص يدوي</option><option value="NONE">لا يتم تحميله</option></select></div><div class="field"><label>المورد</label><input name="vendor"></div><div class="field"><label>الوصف</label><input name="description" placeholder="مثال: استئجار مولد لليلة واحدة"></div></div><div class="actions"><button class="btn btn-primary">حفظ المصروف</button><button type="button" class="btn btn-ghost" onclick="closeModal()">إلغاء</button></div></form>`);$('#costForm').onsubmit=async e=>{e.preventDefault();try{await api('/api/operational-cost',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(new FormData(e.target)))});closeModal();toast('تم حفظ المصروف');costs()}catch(err){toast(err.message,'error')}}}
async function subscriberAccount(id){const a=await api('/api/subscribers/'+id+'/account');openModal(`<div class="account-top"><div><h2>${safe(a.subscriber.name)}</h2><div class="account-id">${safe(a.subscriber.code)} · ${safe(a.subscriber.building_name||'')} · ${safe(a.subscriber.unit_code||'')}</div></div><div class="summary-box"><div class="label">الرصيد الحالي</div><div class="balance-large">${money(a.balance)}</div></div></div><div class="actions"><button class="btn btn-gold" id="editFromAccount">تعديل بيانات الساكن</button><button class="btn btn-primary" onclick="window.print()">طباعة / PDF</button></div><div class="grid2" style="margin-top:15px"><div class="card section"><div class="section-head"><div><h2>قراءات الماء</h2><p>السابق والحالي والاستهلاك.</p></div></div><div class="table-wrap"><table class="table"><thead><tr><th>الفترة</th><th>السابقة</th><th>الحالية</th><th>الاستهلاك</th><th>القيمة</th></tr></thead><tbody>${a.readings.map(r=>`<tr><td>${safe(r.label)}</td><td>${r.previous_reading??'-'}</td><td>${r.current_reading??'-'}</td><td>${r.consumption==null?'-':fmt(r.consumption,3)}</td><td>${money(r.charge_amount)}</td></tr>`).join('')||`<tr><td colspan="5" class="empty">لا توجد قراءات.</td></tr>`}</tbody></table></div></div><div class="card section"><div class="section-head"><div><h2>الدفعات</h2><p>آخر الدفعات.</p></div></div><div class="table-wrap"><table class="table"><thead><tr><th>التاريخ</th><th>المبلغ</th><th>الطريقة</th></tr></thead><tbody>${a.payments.map(p=>`<tr><td>${fmtDate(p.payment_date)}</td><td class="strong">${money(p.amount)}</td><td>${safe(p.method)}</td></tr>`).join('')||`<tr><td colspan="3" class="empty">لا توجد دفعات.</td></tr>`}</tbody></table></div></div></div><div class="card section" style="margin-top:13px"><div class="section-head"><div><h2>كشف الحساب</h2><p>كل حركة تفسّر الرصيد الحالي.</p></div></div><div class="table-wrap"><table class="table"><thead><tr><th>التاريخ</th><th>الحركة</th><th>مدين</th><th>دائن</th><th>البيان</th></tr></thead><tbody>${a.ledger.map(r=>`<tr><td>${safe((r.created_at||'').slice(0,10))}</td><td>${safe(r.transaction_type)}</td><td>${money(r.debit)}</td><td>${money(r.credit)}</td><td>${safe(r.description||'')}</td></tr>`).join('')||`<tr><td colspan="5" class="empty">لا توجد حركات.</td></tr>`}</tbody></table></div></div>`);$('#editFromAccount').onclick=()=>{closeModal();showSubForm(Number(id))}}
function openModal(h){$('#modalBody').innerHTML=h;$('#modal').classList.remove('hidden');$('#modal').setAttribute('aria-hidden','false')}function closeModal(){$('#modal').classList.add('hidden');$('#modal').setAttribute('aria-hidden','true')}
function navigate(v){state.view=v;$$('.nav-item').forEach(b=>b.classList.toggle('active',b.dataset.view===v));const fn={dashboard,subscribers,periods,costs,payments,reports}[v]||dashboard;fn().catch(e=>toast(e.message,'error'));$('#sidebar').classList.remove('open')}
$('#modalClose').onclick=closeModal;$('#refreshBtn').onclick=()=>navigate(state.view);$('#quickPaymentBtn').onclick=async()=>{state.subscribers=await api('/api/subscribers');showPaymentForm()};$$('.nav-item').forEach(b=>b.onclick=()=>navigate(b.dataset.view));document.addEventListener('click',e=>{const s=e.target.closest('.btn-view-sub');if(s)subscriberAccount(s.dataset.id);const ed=e.target.closest('.btn-edit-sub');if(ed)showSubForm(ed.dataset.id)});document.addEventListener('keydown',e=>{if(e.key==='Escape')closeModal()});$('#modal').addEventListener('click',e=>{if(e.target.id==='modal')closeModal()});$('#mobileMenu').onclick=()=>$('#sidebar').classList.toggle('open');window.closeModal=closeModal;window.subscriberAccount=subscriberAccount;navigate('dashboard');
