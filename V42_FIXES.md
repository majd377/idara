V42 PDF layout
- Reduced the isolated PDF rendering scale to 75% so the full report table fits more reliably without clipped sections.
- Kept the same isolated print HTML pipeline that fixed the blank-page issue.
- Updated service-worker cache version to V42 so the PDF layout change is picked up after deployment.
