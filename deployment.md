# Deployment

## Local
npm install
npm start

ثم: http://localhost:3000

## Production
يفضل PostgreSQL + object storage + reverse proxy/HTTPS + secrets manager. النسخة الحالية تعمل محليًا بـ SQLite لتسهيل البدء.
