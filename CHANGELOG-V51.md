# سجل التحديثات — V51

## 1) إصلاح تنزيل PDF المقطوع
**السبب الحقيقي:** المكتبة القديمة (`html2pdf.js` من CDN خارجي) كانت تعتمد على خاصية غير موثوقة
لتفادي قطع الصفوف بين الصفحات، فكانت فعليًا تقطع الصف نص نص بين صفحتين.

**الحل:** بنيت نظام تصدير جديد بالكامل (`pdf-export.js`) يقيس ارتفاع كل صف فعليًا **قبل**
توزيعه على الصفحات، بحيث لا يُقسَّم أي صف أبدًا بين صفحتين — يُنقل الصف كاملًا للصفحة التالية
بدلًا من قصّه. تم اختباره فعليًا بمتصفح Chromium حقيقي (headless) بجدول من 55 صفًا وهميًا، وتم
تحويل الـ PDF الناتج لصور والتأكد بصريًا أن كل صف يظهر كاملًا وبدون أي قطع.

المكتبات (`jsPDF`, `html2canvas`, `xlsx-js-style`) أصبحت مرفقة محليًا داخل مجلد `vendor/` بدل
تحميلها من CDN خارجي — أسرع، تعمل بدون إنترنت، وما بتتعطل إذا انقطع أو تباطأ مصدر خارجي.

## 2) إصلاح اختلاف أرقام الساكن عن أرقام المدير (والبطء المرافق له)
**السبب الحقيقي:** كانت دالة حساب فاتورة المياه للساكن (`currentWaterCharge`) — وأيضًا صفحة
"المياه" الخاصة بالساكن ولوحة القيادة — تتجاهل الأرقام الجاهزة التي حسبها وثبّتها المدير
(`chargeAmount`, `unitPrice`) وتحاول **إعادة حساب** السعر من الصفر عبر دالة `currentTotals()`.
هذه الدالة تحتاج بيانات كل السكان والعمارة كاملة لتحسب سعر الكوب الصحيح — لكن حساب الساكن،
بسبب قواعد الأمان، ما عنده إلا بياناته هو فقط. فكانت النتيجة رقمًا مختلفًا تمامًا (وغالبًا أكبر)،
مع حساب إضافي ثقيل يبطئ الموقع بلا داعٍ.

**الحل:** كل الأماكن الثلاثة صارت تقرأ الرقم الجاهز المخزّن مباشرة (نفس الرقم الذي يراه المدير)
بدل إعادة حسابه. تم التحقق بمتصفح حقيقي مع بيانات وهمية: كشف حساب المدير أظهر **33.9 ₪** لأسبوع
معيّن، وصفحة المياه للساكن نفسه أظهرت **نفس الرقم تمامًا 33.9 ₪** لنفس الأسبوع.

## 3) صلاحية "المشاهد"
تم التحقق: صلاحية المشاهد مضبوطة أصلًا بشكل صحيح — يرى كل الصفحات ما عدا "الإعدادات والصلاحيات"،
ولم تكن بحاجة لتعديل.

## 4) السرعة والتأثيرات والأمان
- **تخزين مؤقت للحسابات:** كشف حساب كل ساكن (`subscriberFinancialSeries`) صار يُحسب مرة واحدة
  فقط لكل تحميل صفحة بدل إعادة حسابه عدة مرات (التقارير + نافذة الحساب + كشف الساكن).
- **انتقالات سلسة:** إضافة تأثير fade/slide ناعم عند التنقل بين الأقسام، وتأثيرات hover/active
  محسّنة للأزرار وعناصر القائمة الجانبية والبطاقات.
- **حماية CSP (Content-Security-Policy):** أضفت سياسة أمان صارمة تمنع أي سكربت أو إطار خارجي غير
  مصرّح به من العمل حتى لو حصل اختراق أو حقن بيانات — تم اختبارها والتأكد أنها **لا تكسر** تسجيل
  الدخول أو عمل الموقع (نقلت سكربت داخلي صغير كان بالصفحة الرئيسية لملف خارجي منفصل حتى تسمح
  السياسة الصارمة بتشغيله بأمان).
- ⚠️ **مهم:** الـ CSP تم اختبارها في بيئة معزولة بدون اتصال حقيقي بـ Firebase/Google (هالبيئة ما
  عندها وصول للإنترنت لهاي الخدمات). لازم تتأكد بعد الرفع إن تسجيل الدخول بجوجل وقراءة/كتابة
  Firestore شغّالين طبيعي؛ إذا صار أي حظر غير متوقع بالمتصفح (Console)، ابعتلي رسالة الخطأ
  وبعدّل السياسة فورًا.

## 5) دقة الأرقام والأرقام الإنجليزية
تم التحقق أن هذا كان منفّذ أصلًا بشكل صحيح بالكود: المبالغ بالشيكل تُعرض لمنزلة عشرية واحدة
بالضبط، والمياه (الكوب) لثلاث منازل عشرية، وكل الأرقام بصيغة `en-US` (أرقام إنجليزية 0-9) في كل
أنحاء الموقع — بدون أي تقريب على مستوى الحسابات الداخلية (التقريب فقط عند العرض).

## ما لم يُنفَّذ بعد (يحتاج نقاش/وقت إضافي)
- اختبار حمل فعلي بـ 1000-2000 مستخدم متزامن (يحتاج بيئة Firebase حقيقية، ما ينعمل بمحاكاة محلية)
- نسخة احتياطية تلقائية/تصدير كامل للبيانات بصيغة JSON من داخل الإعدادات (ميزة أمان بيانات إضافية)
- مراجعة شاملة لكل Methods بالكود سطرًا سطرًا (تم فحص المسارات الحرجة والمرتبطة بالمشاكل
  المذكورة تحديدًا، وتم اختبارها فعليًا بمتصفح؛ فحص شامل 100% لكل دالة بالملف يحتاج جلسة عمل
  منفصلة لأن حجم الملف كبير جدًا)

---

# تصحيح — V51.1 (Hotfix)

بعد النشر، وردت ثلاث ملاحظات فحصتها بالكود فعليًا:

## 1) اختلاف رقم "حسابي" للساكن عن رقم المدير — لم يكن مصلحًا بالكامل
V51 أصلح هذه المشكلة في **ثلاثة** أماكن (`currentWaterCharge`، `renderResidentWater`،
`renderReadOnlyDashboard`) لكن فاتت **دالة رابعة** بنفس الخلل تمامًا: `residentWaterStoredForPeriod`،
وهي الدالة الوحيدة المستخدمة في صفحة "حسابي" الخاصة بالساكن (`renderResidentAccountReport`) **وفي
بطاقة "الرصيد/المديونية" على لوحة الساكن الرئيسية**. كانت لا تزال تستدعي `currentTotals()` التي
تحتاج بيانات كل السكان والعمارة، بينما جلسة الساكن — بسبب `firestore.rules` — لا ترى إلا قراءتها
هي فقط، فينتج سعر ومبلغ مُضخَّمين بشكل كبير (تحققت من هذا مقارنة بقواعد `firestore.rules`: الساكن
يرى كل `costs` لكن قراءات `readings` فقط الخاصة به، فيقسم تكلفة العمارة كاملة على استهلاكه هو فقط).
تم إصلاحها بنفس نمط الإصلاح المستخدم بالفعل بثلاث أماكن أخرى، والتحقق منه حسابيًا: نفس القراءة
المحفوظة (استهلاك 1.207 كوب، `chargeAmount` محفوظ = 35.0 ₪) تُعطي الآن **35.0 ₪ في الحالتين**
بدل 1,814.5 ₪ سابقًا في صفحة الساكن.

## 2) الموقع يبدو "مغبشًا"
السبب: تأثير الانتقال السلس الجديد بين الصفحات (`#app.view-fade-out{opacity:.4}`) كان يُضاف قبل
`await loadData()` و `render()` بدون `try/finally`. أي خطأ أثناء التحميل أو العرض (رفض صلاحية من
Firestore، انقطاع شبكة، أو أي استثناء JS في أي صفحة) كان يوقف التنفيذ ويترك الشاشة **بأكملها عالقة
عند شفافية 40% بشكل دائم** — وهذا بالضبط ما يبدو وكأن الموقع "مغبش". تم لف `loadData()`/`render()`
بـ `try/finally` بحيث تُزال حالة التعتيم دائمًا مهما حدث، مع إظهار رسالة خطأ واضحة للمستخدم بدل
الفشل الصامت.

## 3) تحذيرات CSP لملفات gstatic.js.map
غير مرتبطة بأي خلل وظيفي — المتصفح فقط يحاول تحميل خرائط مصادر (source maps) لمكتبة Firebase
لأغراض التصحيح في أدوات المطور، وسياسة `connect-src` لم تكن تسمح بذلك. أُضيف `https://www.gstatic.com`
إلى `connect-src`. لا علاقة لهذا بعمل الموقع الفعلي.
